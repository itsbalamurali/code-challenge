apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-challenge.fullname" . }}
  labels:
    {{- include "code-challenge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-challenge.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "code-challenge.fullname" . }}-test-connection"
  labels:
    {{- include "code-challenge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "code-challenge.fullname" . }}:{{ .Values.service.port }}/versionz']
  restartPolicy: Never
